name: 'qwen_vl_llm'
description: 'qwen_vl_llm'
model_name: 'qwen-vl-max-latest'
temperature: 0.5
max_tokens: 2000
#
# When agentUniverse starts, it will automatically read the `DASHSCOPE_API_KEY` from the environment variables
# and assign its value to api_key during YAML configuration parsing.
# The same logic applies to other parameters with placeholders below, such as organization, api_base, and proxy.
#
api_key: '${DASHSCOPE_API_KEY}'
api_base: 'https://dashscope.aliyuncs.com/compatible-mode/v1'
proxy: '${DASHSCOPE_PROXY}'
organization: '${DASHSCOPE_ORGANIZATION}'
metadata:
  type: 'LLM'
  module: 'agentuniverse.llm.default.qwen_openai_style_llm'
  class: 'QWenOpenAIStyleLLM'